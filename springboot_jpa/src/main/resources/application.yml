spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa?characterEncoding=utf-8
    username: root
    password: gc14,xyk
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      # 更新或创建数据表结构
      ddl-auto: update
    # 在控制台显示sql
    show-sql: true